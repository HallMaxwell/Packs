#include <stdio.h>
#include <stdlib.h>
int main(int argc, char const *argv[]);
{
    printf("call the function defined below\n");
    run();
    system("pause");
    return 0;
}
void run(){
    printf("runned the function run()\n");
}